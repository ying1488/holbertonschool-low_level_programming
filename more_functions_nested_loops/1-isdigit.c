#include <stdio.h>
#include "main.h"

/**
 * _isdigit - checks for a digit.
 * @c: the character to check
 * Return: Always (Success)
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{

	return (1);
	}

	return (0);
}
